apiVersion: apps/v1
kind: Deployment
metadata:
  name: geo-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: geo-service
  template:
    metadata:
      labels:
        app: geo-service
    spec:
      containers:
      - name: geo-deployment
        image: claudia179/geo-service-img:latest
        ports:
        - containerPort: 5000
        imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: geo-service
spec:
  selector:
    app: geo-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
